// Scala program to appending a character to
// an immutable string using "+=" operator

object Sample {
  def main(args: Array[String]) {
    val str = new StringBuilder("Indi");

    //Appending a character to string
    str += 'a';

    println(str);
  }
}
