// Scala program to get the index of
// last occurrence of specified substring

object Sample {
  def main(args: Array[String]) {
    var str: String = "Banana";
    var index: Int = 0;

    index = str.lastIndexOf("na");
    printf("Index of last occurrence of substring 'na' is: %d\n", index);
  }
}
